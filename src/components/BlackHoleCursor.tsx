/**
 * BlackHoleCursor — DEPRECATED
 *
 * Per the Gravity Design Manifesto:
 * "Never create a fake cursor. Never render decorative cursor rings.
 *  The environment should react. The cursor should remain invisible as a design element."
 *
 * The gravity field is invisible. Elements react to it. The cursor is the OS cursor.
 */
export default function BlackHoleCursor() {
  return null
}
