/**
 * CustomCursor — DEPRECATED
 *
 * Per the Gravity Design Manifesto:
 * "The native operating system cursor must remain unchanged. Never replace it."
 *
 * This component is intentionally empty.
 * The cursor gravity field lives in GravityContext and acts invisibly.
 */
export default function CustomCursor() {
  return null
}
